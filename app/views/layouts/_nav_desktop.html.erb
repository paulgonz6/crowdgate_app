<nav class="navbar navbar-default hidden-xs">
  <div class="container-fluid">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <span class="navbar-brand">
        <%= link_to "Crowdgate", root_url %>
      </span>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-left">
        <li id="findTailgateButton">
          <%= link_to ('Find a Tailgate <i class="fa fa-chevron-down"></i>').html_safe, '#' %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user.present? && current_user.host_status? %>
        <li><%= link_to "List a Tailgate", new_tailgate_url%></li>
        <% else %>
        <li><%= link_to "Become a Host", new_host_url%></li>
        <% end %>

        <li><%= link_to "How It Works", how_it_works_url %></li>

        <% if current_user.present? && current_user.admin? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Admin <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Index of Events", events_url%></li>
            <li><%= link_to "New Events", new_event_url%></li>
            <li class="divider"></li>
            <!-- <li><a href="/users/3">Sample User</a></li> -->
            <li><%= link_to "Users", users_url%></li>
            <li class="divider"></li>
            <li><a href="/teams">Teams</a></li>
          </ul>
        </li>
        <% end %>

        <% if current_user.present? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
            aria-expanded="false">
            Hi, <%= current_user.try(:short_name) %><span class="caret"></span>
          </a>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to "Profile", user_url(current_user.id) %>
                <%= link_to "Account Details", account_url %>
              </li>
              <li class="divider"></li>
              <li>
                <%= link_to "Sign Out", destroy_user_session_url, 'data-method' => "delete" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <!-- <%= link_to "Sign Up", new_user_registration_url %> -->
          </li>
          <li class="modal-text-link" data-toggle="modal" data-target="#log-in-modal"><a>Log In</a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
