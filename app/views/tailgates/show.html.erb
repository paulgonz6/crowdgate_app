<div class="container">
  <div class="row">
    <div class="col-md-8">
      <div class="page-header">
        <h3>Tailgate for Army Game! Cornhole, food, and more...</h3>
        <h4><small>Penn State vs. Army, Beaver Stadium, University Park, PA, Saturday, Oct. 3</small></h4>
      </div>
    </div>
    <%= render 'tailgates/booking_block' %>
  </div>

  <%= render 'tailgates/tailgateinfo' %>
  <%= render 'tailgates/pictures' %>
  <%= render 'tailgates/host_info' %>
  <%= render 'tailgates/reviews' %>

</div>